# 프로젝트 개발 컨벤션

## 📂 파일 구조
코딩 컨벤션: 디렉토리 구조는 **View(기능) 단위**로 구성합니다.

- **기능별 폴더 (ex. 로그인, 온보딩 등)**
  - 메인 컴포넌트 폴더 내부에 각 기능별 폴더를 생성합니다.
  - 해당 기능 안에서 다시 컴포넌트, 모델, 뷰, 뷰모델 등을 위치시킵니다.
  - *예시 (로그인 기능)*
    ```text
    Login/
    ├── LoginButton.swift (컴포넌트)
    ├── LoginModel.swift
    ├── LoginView.swift
    └── LoginViewModel.swift
    ```

---

## 💻 개인 작업 및 브랜치 규칙

### 1. 이슈(Issue) 파기
- 작업 시작 전 반드시 이슈를 생성합니다.
- 예시: `feat: ARView UI 구현`

### 2. 브랜치(Branch) 생성
- 브랜치는 `main`이 아닌 **`develop` 브랜치에서 분기**시킵니다.
- **브랜치 네이밍 규칙**: `타입/#이슈번호-작업내용`
  - 예시: `feat/#12-login`

### 3. 작업 및 커밋(Commit)
- **커밋은 자주, 작은 단위로!** (너무 큰 단위로 커밋하지 않도록 주의합니다.)
- 작업이 다 끝나면 `develop` 브랜치로 Pull Request(PR)를 올립니다.

### 4. Pull Request (PR) 및 병합(Merge)
- **무조건 `develop` 브랜치로 PR**을 날려 코드 리뷰를 진행합니다.
- **Merge 조건**: 최소 1명 이상 검토 및 승인 시 Merge 합니다.
- **이슈 컨벤션**: 템플릿을 사용하며, Reviewers에는 팀원 전원, Assignees에는 본인을 지정합니다.
- *참고: 최종 작업이 완료된 후 릴리즈 시점에 `develop` 브랜치를 `main`에 머지합니다.*

---

## 💬 커밋 메시지 컨벤션

### 커밋 종류 (Prefix)
- `feat` : 새로운 기능 추가
- `fix` : 버그 수정
- `style` : 코드 스타일 변경 (코드 포매팅, 세미콜론 누락 등 로직에 영향 없는 수정)
- `design` : 사용자 UI 디자인 변경
- `refactor` : 코드 리팩토링 (기능 변화 없음)
- `build` : 빌드 관련 파일 수정
- `chore` : 자잘한 수정이나 빌드 업데이트, 패키지 매니저 수정 등
- `rename` : 파일 혹은 폴더명을 수정만 한 경우
- `remove` : 파일을 삭제만 한 경우

### 커밋 메시지 예시
- `chore: 프로젝트 초기 세팅`
- `feat: 프로필 화면 UI 구현`
- `fix: iOS 17에서 버튼 클릭 오류 수정`
- `design: 로그인 화면 레이아웃 조정`
- `docs: README에 프로젝트 소개 추가`
